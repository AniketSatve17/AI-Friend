# /services

This directory contains business logic, helpers, and connections to external APIs.

- **aiService.js**: Handles all logic for connecting to and getting responses from the Google Gemini AI.
- **dbService.js**: (Future) Will handle all database connection and query logic.
- **authService.js**: (Future) Will handle logic for passwords, tokens, etc.